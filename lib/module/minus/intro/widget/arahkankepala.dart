import 'package:flutter/material.dart';

class ArahkanKepala extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[Text('Kepala')],
    );
  }
}
